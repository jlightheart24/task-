# macOS App (SwiftUI)

This will host the native SwiftUI macOS app.

Planned integration:
- Go shared core via gomobile bind (XCFramework)
- macOS Keychain for key storage
- Offline-first local SQLite managed by Go core
