# iOS App (SwiftUI)

This will host the native SwiftUI iOS app.

Planned integration:
- Go shared core via gomobile bind (XCFramework)
- Keychain for key storage
- Offline-first local SQLite managed by Go core
