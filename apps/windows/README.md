# Windows App (WinUI 3 .NET)

This will host the native WinUI 3 (.NET) Windows app.

Planned integration:
- Go shared core exposed via C-ABI (DLL)
- P/Invoke bindings in C#
- Windows Credential Manager for key storage
- Offline-first local SQLite managed by Go core
